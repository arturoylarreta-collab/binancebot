[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "crypto-scalper"
version = "0.8.1"
description = "Modular, quantitative, async crypto scalping research system (FASE 1/2: architecture + market data, FASE 3: signals, FASE 4: ML filter, FASE 5: Risk Engine, FASE 6: Execution Engine, FASE 7: Paper Trading offline, FASE 8: Backtesting, FASE 8.5: Observability + Telegram alerts + Dashboard)"
requires-python = ">=3.11"
dependencies = [
    "numpy>=1.26",
    "aiohttp>=3.9",
    "python-dotenv>=1.0",
    "scikit-learn>=1.9",
    "joblib>=1.3",
    "streamlit>=1.57",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
]

[tool.setuptools.packages.find]
include = ["crypto_scalper*"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
markers = [
    "slow: longer-running end-to-end ML fits (deselect with -m 'not slow')",
]